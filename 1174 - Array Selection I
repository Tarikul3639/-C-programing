#include <stdio.h>
int main()
{
    float array[100];
    float i, n;
    //scanf("%d", &n);
    for (i = 0; i < 100; i++)
    {
        scanf("%f",&n);
        if(n<=10)
        {
            printf("A[%.f] = %.1f\n", i, n);
        }
    }
    return 0;
}
