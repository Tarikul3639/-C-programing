#include<stdio.h>
int main()
{
    char str1[100];
    char str2[100];

    gets(str1);

    int i,j,sum=0;

    for(i=0; str1[i]>'\0'; i++)
    {
    }

    printf("%d\n",i);

    for(j=0,i=i-1; i>=0; i--,j++)
    {
        str2[j]=str1[i];
    }
    printf("%s\n",str2);
    return 0;
}
