#include <stdio.h>
int main()
{
    int p=0,i, n, array[20];

    for(i=0; i<20; i++)
    {
        scanf("%d", &array[i]);
    }
    for(i=19; i>=0; i--)
    {
        printf("N[%d] = %d\n", p++, array[i]);
    }


    return 0;
}
